import "./globals.css";

export const metadata = {
  title: "Website Opportunity Finder AI - Build Plan",
  description:
    "Comprehensive implementation blueprint for the Website Opportunity Finder AI agent."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
